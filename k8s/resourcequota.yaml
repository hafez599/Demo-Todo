apiVersion: v1
kind: ResourceQuota
metadata:
  name: lastone-quota
  namespace: lastone
  labels:
    app: lastone
spec:
  hard:
    requests.cpu: 1
    requests.memory: 1Gi
    limits.cpu: 2
    limits.memory: 2Gi
    pods: "20"
