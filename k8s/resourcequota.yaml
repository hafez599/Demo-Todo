apiVersion: v1
kind: ResourceQuota
metadata:
  name: hafoza-quota
  namespace: hafoza
  labels:
    app: hafoza
spec:
  hard:
    requests.cpu: 1
    requests.memory: 1Gi
    limits.cpu: 2
    limits.memory: 2Gi
    pods: "20"
