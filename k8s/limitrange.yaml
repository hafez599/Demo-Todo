apiVersion: v1
kind: LimitRange
metadata:
  name: lastone-limits
  namespace: lastone
  labels:
    app: lastone
spec:
  limits:
    - type: Container
      defaultRequest:
        cpu: 100m
        memory: 128Mi
      default:
        cpu: 250m
        memory: 256Mi
