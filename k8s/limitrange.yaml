apiVersion: v1
kind: LimitRange
metadata:
  name: hafoza-limits
  namespace: hafoza
  labels:
    app: hafoza
spec:
  limits:
    - type: Container
      defaultRequest:
        cpu: 100m
        memory: 128Mi
      default:
        cpu: 250m
        memory: 256Mi
